import { colors } from "./colors";
import { fonts } from "./fonts";
import { space } from "./space";
import { shadow } from "./shadow";
import { size } from "./size";

const theme = { colors, fonts, space, shadow, size };

export default theme;
